version: "3.9"
    
services:
  backend:
    image: ms-backend:latest
    build:
      context: ./
      dockerfile: ./Dockerfile-backend 
    ports:
      - "9008:9000"
    volumes:
      - db_data:/opt/ms/tmp
    
volumes:
  db_data: {}


