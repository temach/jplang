import {createSlice} from '@reduxjs/toolkit';

const searchrulesSlice = createSlice({
    name: 'searchrules',
    initialState: {},
    reducers: {
    },
    extraReducers: {
    }
});

export default searchrulesSlice.reducer;
